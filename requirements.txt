# Exact environment used to create and independently replay the committed
# V6-lite evidence. MuJoCo is pinned because its version participates in the
# runtime-contract identity hash.
numpy==1.21.6
scipy==1.11.2
mujoco==3.3.2
matplotlib==3.8.0
Pillow==10.2.0
